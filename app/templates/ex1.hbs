{{page-title "Ex1"}}
<div class="card">
  <div class="card-header bg-primary text-light">Exercice 1 : Note Application</div>
<div class="card-body">

  <label>
    <Textarea class="form-control" @value={{this.content}}/>
  </label>

  <div class="alert-info">il reste {{this.size}} caractères disponibles</div>
  <div class={{this.style}}>test</div>
</div>
</div>
{{outlet}}